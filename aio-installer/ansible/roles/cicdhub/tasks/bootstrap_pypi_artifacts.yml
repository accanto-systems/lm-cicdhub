- name: Create PyPi Directory
  file:
    path: "/tmp/bootstrap_pypi"
    state: directory

- name: Upload PyPi artifacts
  include_tasks: bootstrap_pypi_artifact.yml
  vars:
    pypi_project_def: "{{ item }}"
  loop: "{{ bootstrap_pypi_artifact_defs }}"