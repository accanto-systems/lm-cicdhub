name: docker-network
vnfcs:
  definitions:
    docker-network:
      directory: docker-network
  packages:
    ansible-rm-vnfcs:
      packaging-type: ansible-rm
      executions:
      - vnfc: docker-network
